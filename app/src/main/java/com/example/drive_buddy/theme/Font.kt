package com.example.drive_buddy.theme
import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.drive_buddy.R

val MuliRegular = FontFamily(
    Font(R.font.muli_regular)
)
val MuliBlack = FontFamily(
    Font(R.font.muli_black)
)
val MuliBold = FontFamily(
    Font(R.font.muli_bold)
)

val MuliLight = FontFamily(
    Font(R.font.muli_light)
)

val MuliSemiBold = FontFamily(
    Font(R.font.muli_semibold)
)

val MuliExtraBold = FontFamily(
    Font(R.font.muli_extra_bold)
)